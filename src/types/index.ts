/**
 * App-wide TypeScript types. Domain models live in `@/lib/types`; database types in `@/lib/supabase/database.types`.
 */
export * from "@/lib/types";
export type { Database } from "@/lib/supabase/database.types";
